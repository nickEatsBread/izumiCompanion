# izumi Companion 0.2.52

- When a release's default audio track uses a codec this TV cannot decode, playback switches to a decodable track in the same language, then any decodable track, instead of playing silently or failing.
- Worker 1.14.3 refuses torrents that contain only trailers or samples, recognises scene trailer and teaser file tokens, and lists sources in the same order as the desktop picker while keeping its own automatic choice.
- Desktop and mobile installers bundle Worker 1.14.3.
