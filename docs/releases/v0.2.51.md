# izumi Companion 0.2.51

- Playback starts from the first settled source result while the private Worker keeps listing releases behind it. The picker keeps filling, and Back no longer abandons the search.
- The loading screen leads with the source being opened; the running source tally is secondary.
- Change source is a wide centred picker showing each listing's logo and name, the delivery kind, the release name, and its quality, size, seeders and group.
- Worker 1.14.2 prepares gateway playback routes through your own debrid provider instead of handing the TV a link bound to the Worker's network address, and uses the catalogue's release year and runtime to reject a same-title production from another era.
- Desktop and mobile installers bundle Worker 1.14.2.
